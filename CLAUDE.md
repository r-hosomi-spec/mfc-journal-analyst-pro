# mfc-journal-analyst-pro — 事務所共通指示書

## プロジェクト概要

本リポジトリは税理士事務所の業務をClaude Codeで自動化するための基盤です。

**主な業務:**
- MFクラウド会計向け仕訳CSV作成（振替伝票・仕訳帳インポート形式）
- 月次面談議事録・面談準備シートの作成
- Chatwork報告文の生成

**技術参照:**
- MFクラウド会計スキル仕様（データ分析・引継書作成の詳細）: @SKILL.md
- 共通税区分・勘定科目ルール: @.claude/rules/tax_common.md
- Chatwork・議事録テンプレート集: @.claude/rules/prompt_templates.md

---

## 絶対ルール（全業務共通）

### ガバナンス：曖昧な取引の扱い

- **推論で仕訳を確定させない。** 取引の意図が不明・証拠不十分な場合は「要確認リスト」に分類する。
- 要確認リストの出力形式:

  ```
  ## 要確認リスト
  | # | 取引日 | 摘要 | 金額 | 不明な理由 | 候補勘定科目A / B |
  |---|--------|------|------|-----------|-----------------|
  ```

- 要確認リストは回答の**最後に独立したセクション**として必ず出力する（0件の場合は「要確認事項なし」と記載）。
- インボイス登録番号が未確認の外注費・支払手数料は、金額にかかわらず要確認リストに含める。

### CSV出力仕様（マネーフォワード向け）

- **エンコーディング: Shift-JIS (CP932) 固定。** UTF-8・UTF-8 BOM等への変更は絶対に行わない。
- 改行コード: CRLF（Windows標準）
- 数値: カンマなし整数（例: `110000`）
- 日付: `YYYY/MM/DD` 形式
- 列順・列名: マネーフォワード公式インポート仕様に従う（仕訳帳形式 / 振替伝票形式で異なる）
- Pythonでの書き込み: 必ず `encoding='cp932', errors='replace'` を指定する

### データ保護

- 顧客情報・口座番号・個人情報・認証トークンを含むファイルはコミットしない。
- `.gitignore` に記載された除外ルール（`journals_*.json`、`.mfc_token.json` 等）を厳守する。
- 生成物（CSV・レポート・議事録）は `output/` ディレクトリに配置する。

### 出力の品質基準

- 数値は根拠を明示する（例: `合計110,000円 = 税抜100,000円 + 消費税10,000円`）。
- 仮定を置いた場合は冒頭または該当箇所に明記する（例: `※ 税抜処理と仮定して計算しています`）。
- 不確実な情報は断言しない。情報源がある場合は出典を示す。

---

## 業務別の作業ディレクトリとルール

| 業務 | 作業ディレクトリ | 追加ルール |
|------|-----------------|-----------|
| 仕訳CSV作成 | `accounting/` | `accounting/CLAUDE.md` が加算される |
| 議事録・面談準備 | `minutes/` | `minutes/CLAUDE.md` が加算される |
| Chatwork報告文 | `chatwork/` | `chatwork/CLAUDE.md` が加算される |

各サブディレクトリで作業する際、Claude Codeはそのディレクトリの `CLAUDE.md` を自動的に読み込み、本ファイルのルールに加算します。

---

## コーディング規約（Pythonスクリプト）

- CSV読み込みのエンコーディング自動検出順: `utf-8-sig` → `cp932` → `utf-8`
- CSV書き込みは必ず `encoding='cp932', errors='replace'` を指定
- DataFrameの列名は日本語可（既存スクリプト `scripts/` の命名規則に合わせる）
- 既存スクリプトの関数を優先的に再利用する（`load_journals.py` の `load_df()`・`enrich_partners()` 等）
